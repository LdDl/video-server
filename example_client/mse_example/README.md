# Example of MSE-based (websockets) player on VueJS

## Install dependencies
```shell
npm install
```

### Run debugging mode with hot-reload ability
```shell
npm run dev
```

### Prepare for build mode and run
```shell
npm run build
npm run start
```