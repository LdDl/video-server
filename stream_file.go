package videoserver

import (
	"github.com/google/uuid"
)

// runLocalFileStream runs stream from file
func (app *Application) runLocalFileStream(streamID uuid.UUID, url string, loop, hlsEnabled, archiveEnabled bool, streamVerboseLevel VerboseLevel) error {
	panic("runLocalFileStream is not implemented")
	return nil
}
